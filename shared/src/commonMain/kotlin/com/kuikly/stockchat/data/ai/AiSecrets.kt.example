package com.kuikly.stockchat.data.ai

/**
 * 本地密钥。复制本文件为同目录 `AiSecrets.kt` 后填写，勿提交 `AiSecrets.kt`。
 */
object AiSecrets {
    /** 百炼 Key：按量 `sk-` 或 Token Plan `sk-sp-`，网关会按前缀自动选择。 */
    const val VOLC_API_KEY = ""
}
